import React from 'react';

const Blog = () => {
    return (
        <div>
            
            <h1 className='mb-4 text-lg'>1.context api⁇   </h1>
            <h1>Ans:  Normally when we pass data varius component using props, but when we have 
                more then 10 component or above it will more easy to pass dada using 
                context api for certain component. </h1>
                <h1 className='mb-4 mt-2 text-lg'>2.what is semantic tag⁇   </h1>
                <h1>
                Ans:  Semantic HTML is the use of HTML markup to reinforce the semantics,
                 or meaning, of the information in web pages and web applications rather than
                  merely to define its presentation or look
                </h1>
        </div>
    );
};

export default Blog;